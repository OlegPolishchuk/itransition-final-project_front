const RowsPerPage = {
  low: 10,
  middle: 20,
  top: 50,
};

export const usersTablePaginationData = {
  rowsPerPage: [RowsPerPage.low, RowsPerPage.middle, RowsPerPage.top],
  defaultRowPerPage: 10,
  defaultPageNumber: 0,
  defaultLimit: 10,
};
